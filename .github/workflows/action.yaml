name: Sample job on push to main

on:
   push:
      branches: [ "main" ]

jobs:
   build:
     runs-on: ubuntu-latest
     steps:
        - uses: actions/Checkout@v2
        - name: Checkout repo
         
        - name: sample commands
          run: echo "hello"
